{
  "name": "@trano/server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --watch src/index.ts",
    "start": "node src/index.ts",
    "lint": "tsc --noEmit"
  },
  "//": "Node 24 exécute le TypeScript nativement (effacement des types) : plus besoin de tsx, qui lançait un processus esbuild résident de ~77 Mo. Décisif ici — la VM Home Assistant de la Freebox est plafonnée à 1 Go.",
  "dependencies": {
    "@fastify/cors": "^11.0.1",
    "@fastify/static": "^8.1.1",
    "@fastify/websocket": "^11.0.2",
    "@trano/shared": "*",
    "fastify": "^5.3.2"
  },
  "devDependencies": {
    "@types/node": "^22.14.0",
    "typescript": "~5.8.2"
  }
}
